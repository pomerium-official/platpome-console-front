import React from 'react';
import styles from './LoadingFullLayer.module.scss';

const LoadingFullLayer = () => {
  return <div className={`${styles.loadingFullLayer} loadingFullLayer`} />;
};

export default LoadingFullLayer;
