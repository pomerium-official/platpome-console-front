export enum languageCode {
  korean = 'kr',
  english = 'en',
  japanese = 'jp',
  chinese = 'cn',
  spanish = 'es',
}

export enum menuTypeSnb {
  tree = 'tree',
  panel = 'panel',
}

export enum menuTypeGnb {
  tab = 'tab',
  bar = 'bar',
  oneDepthOnly = 'oneDepthOnly',
}

export enum logoPosition {
  gnb = 'gnb',
  snb = 'snb',
}

export enum position {
  left = 'LEFT',
  right = 'RIGHT',
}

export enum surveyShowMode {
  normal = 'NORMAL',
  modal = 'MODAL',
}

export enum surveyRegistrationMode {
  add = 'ADD',
  modify = 'EDIT',
  ready = 'READY',
}

export enum openLayoutMode {
  public = 'PUBLIC',
  private = 'PRIVATE',
}
